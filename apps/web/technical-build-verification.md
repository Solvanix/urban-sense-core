# التحقق التقني للبناء والمستودع

**تاريخ التحقق:** 14 أغسطس 2026  
**المشروع المُدار:** `urban-sense-core-app`  
**مستودع GitHub:** [`Solvanix/urban-sense-core`](https://github.com/Solvanix/urban-sense-core) — فرع `main` خاص

## النسخة المصدرية وحالة المزامنة

النسخة التشغيلية الأحدث هي المشروع المُدار في `/home/ubuntu/urban-sense-core-app`، إذ تحتوي التعديلات المتحققة في الواجهة، التفويض، ودورة العمل والتي لم تكن كلها موجودة في نسخة GitHub ذات الالتزام `6646820`. تمت مطابقة ملفات التطبيق ذات الصلة ثم مزامنتها إلى المسار `apps/web` في المستودع، مع استبعاد ملفات بيئة التشغيل وسجلات الاسترداد أو السجلات التسويقية الخاصة.

| البند | النتيجة |
|---|---|
| النسخة المُدارة | تعمل على فرع `main` الخاص بالمشروع وتحتوي أحدث التغييرات المتحققة |
| مستودع GitHub | خاص، ووجهته `Solvanix/urban-sense-core` على `main` |
| مسار التطبيق في المستودع | `apps/web` |
| ملفات تمت مزامنتها | مسارات React، صفحة الدعوة التجريبية، إجراءات التفويض، اختبار دورة العمل، وتوثيق التحقق |
| ملفات لم تُزامن | إعدادات البيئة، وسجلات LinkedIn، وإرشادات استرداد الحساب الخاصة |

## نتائج فحص الجودة

أجري التحقق أولًا داخل المشروع المُدار، ثم تكرر داخل `apps/web` بعد تثبيت التبعيات من ملف القفل المعتمد. فشل التشغيل الأول في المستودع فقط بسبب عدم وجود `node_modules`، وليس بسبب عيب في الشيفرة. بعد `pnpm install --frozen-lockfile` نجحت جميع الفحوص.

| الفحص | المشروع المُدار | `apps/web` في المستودع |
|---|---:|---:|
| `pnpm check` | ناجح | ناجح |
| `pnpm test` | ناجح | ناجح |
| اختبارات Vitest | 18/18 | 18/18 |
| `pnpm build` | ناجح | ناجح |
| ناتج الخادم | `dist/index.js` بحجم 67.9 كيلوبايت | `dist/index.js` بحجم 67.9 كيلوبايت |

## خطأ Vite المبلّغ عنه

ظهر في السجل القديم خطأ يفيد بعدم القدرة على حل الاستيراد `./pages/OperationsDashboard`. التحقق الحالي يثبت وجود `client/src/pages/OperationsDashboard.tsx` وتسجيل تحديثات HMR لاحقة للملف نفسه، كما نجح فحص الأنواع والبناء الإنتاجي. لذلك صُنِّف الخطأ على أنه **سجل تاريخي سابق** وليس عائقًا حاليًا في البناء.

## تأكيد GitHub

دُفعت المزامنة إلى GitHub في الالتزام [`c994901`](https://github.com/Solvanix/urban-sense-core/commit/c9949017b69a787b168038d1bb2f5d2c111fdc79). أتم خط **Web quality** التشغيل [`31804499434`](https://github.com/Solvanix/urban-sense-core/actions/runs/31804499434) بنجاح، ويتضمن تثبيت التبعيات وفحص الأنواع وتشغيل الاختبارات والبناء في المسار `apps/web`.

## عناصر تشغيلية باقية

لا توجد عيوب برمجية مثبتة في فحوص الإصدار الحالية. ما تبقى لا يمكن محاكاته تقنيًا دون الإخلال بالنزاهة أو قيود واجهة خارجية: تقييم حقيقي من مشارك تجريبي، واختيار صور LinkedIn يدويًا في نافذة رفع الخدمة قبل نشر مسودتي شارع الإرسال والطيرة. نُشر منشور إطلاق Urban‑Sense الأول من صفحة SENSE TECH، أما المسودات التي تستخدم صورًا حقيقية مرخصة فما زالت غير منشورة.
